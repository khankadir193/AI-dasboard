- [ ] Diagnose root cause: duplicate handleSend / duplicate in-flight guard lacking.
- [ ] Apply minimal production-safe fix: add an in-flight ref guard inside `useAIChat.handleSend` to prevent concurrent executions.
- [ ] Verify: only one “Analyzing your data...” visible; only one API request per prompt; no duplicate assistant messages; no infinite loading.
